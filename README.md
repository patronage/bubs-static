Optional: 

* If you don't already have it, install 11ty: `yarn global add @11ty/eleventy` or `npm install -g @11ty/eleventy`
* Install gulp globally if you haven't aleady: `yarn global add gulp` or `npm i -g gulp`
* `brew install gs graphicsmagick imagemagick --with-webp`

1. `yarn install`
2. `gulp`